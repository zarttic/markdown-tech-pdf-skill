# Optional dependencies for markdown-tech-pdf
# The script also works without these (uses built-in fallback parser).
markdown>=3.5          # Full spec-compliant Markdown parser (tables, fenced_code, toc, def_list, sane_lists)
